# 7. 厦门大学 2025 年 844 真题逐题技巧与知识连接

## 7.0 使用说明与题源边界

本卷来自第三方扫描资料，卷面标称为厦门大学 2025 年 844《自动控制原理》，并附有第三方参考解析。它不是厦门大学官网发布的试卷或答案。

本文的处理原则是：

1. 题目可以作为训练材料；
2. 所有结论重新从定义推导；
3. 第三方答案只作对照，不作为证明；
4. 发现疑点时明确标出，尤其是第 10 题的能观性结论。

原扫描件见：[厦门大学 2025 年 844 真题及第三方解析](真题原卷/厦门大学2025年844自动控制原理真题及第三方解析（待核验）.pdf)。

---

## 7.1 第一题：桥式 RC 电路求传递函数

### 识别入口

这是“电路建模 + 节点消元”题。图形看起来像桥式网络，最稳的方法不是连续做串并联等效，而是选中间节点电压，直接写节点电流方程。

### 可复用技巧

遇到含跨接电容的网络时，先在复频域使用阻抗或导纳：

$$
Z_C=\frac{1}{sC},
\qquad
Y_C=sC.
$$

然后遵循：

1. 选择非参考节点电压；
2. 对每个节点写 KCL；
3. 联立消去内部节点；
4. 最后检查直流和高频极限。

### 本题推导骨架

设输入、输出和中间节点电压分别为：

$$
U_i(s),
\qquad
U_o(s),
\qquad
U_m(s).
$$

中间节点 KCL 为：

$$
\frac{U_m-U_i}{R_1}
+\frac{U_m-U_o}{R_2}
+sC_2U_m=0.
$$

输出节点 KCL 为：

$$
\frac{U_o-U_m}{R_2}
+sC_1(U_o-U_i)=0.
$$

消去中间节点电压，得到：

$$
\boxed{
\frac{U_o(s)}{U_i(s)}
=
\frac{
R_1R_2C_1C_2s^2+(R_1C_1+R_2C_1)s+1
}{
R_1R_2C_1C_2s^2+(R_1C_1+R_2C_1+R_1C_2)s+1
}.
}
$$

### 两个不用重算的验算

直流时电容开路，因此：

$$
\lim_{s\to0}\frac{U_o(s)}{U_i(s)}=1.
$$

高频时电容近似短路，分子和分母最高次项相同，因此：

$$
\lim_{s\to\infty}\frac{U_o(s)}{U_i(s)}=1.
$$

如果结果的常数项或最高次项不能给出这两个极限，节点方程或消元过程一定有误。

### 易错点

- 跨接电容使网络不再是简单串并联；
- 写 KCL 时所有支路电流必须采用同一流出或流入约定；
- 不要在非零初始条件下直接把电容当作阻抗。本题默认零初值求传递函数。

### 连接之前的知识点

- [经典控制：从微分方程到传递函数](2.经典控制理论（公理化推导与简例）.md#22-传递函数)
- [经典控制：建模检查表](2.经典控制理论（公理化推导与简例）.md#2141-建模)

---

## 7.2 第二题：非零初值微分方程

### 识别入口

题目给出：

$$
\ddot y+2\dot y-3y=e^{-t},
$$

以及：

$$
y(0)=1,
\qquad
\dot y(0)=0.
$$

这是“非零初值 + 外部输入”的完整响应题。不能使用零初值传递函数后只乘输入，因为那样会漏掉零输入响应。

### 可复用技巧

拉普拉斯变换时，把初值项当成独立输入保留：

$$
\mathcal L\{\dot y\}=sY(s)-y(0),
$$

$$
\mathcal L\{\ddot y\}=s^2Y(s)-sy(0)-\dot y(0).
$$

### 本题推导

代入初值：

$$
s^2Y-s+2(sY-1)-3Y=\frac{1}{s+1}.
$$

整理：

$$
Y(s)=
\frac{s^2+3s+3}{(s+1)(s-1)(s+3)}.
$$

部分分式展开：

$$
Y(s)
=-rac{1}{4(s+1)}
+rac{3}{8(s+3)}
+rac{7}{8(s-1)}.
$$

因此：

$$
\boxed{
y(t)
=-rac14e^{-t}
+rac38e^{-3t}
+rac78e^t.
}
$$

### 验算

代入初值：

$$
y(0)
=-rac14+rac38+rac78
=1,
$$

$$
\dot y(0)
=rac14-rac98+rac78
=0.
$$

原齐次方程含正根，因此响应中出现增长项：

$$
e^t.
$$

这与系统不稳定的物理判断一致。

### 易错点

- 传递函数定义采用零初值，但微分方程求解题明确给了非零初值；
- 输入指数项对应的极点不是系统极点；
- 求完时域表达式后必须回代两个初值。

### 连接之前的知识点

- [经典控制：传递函数的零初值边界](2.经典控制理论（公理化推导与简例）.md#221-定义)
- [现代控制：零输入与零状态响应](4.现代控制理论（公理化推导与简例）.md#23-定理-2受迫系统的状态响应)

---

## 7.3 第三题：由二阶阶跃曲线反求系统

### 识别入口

图中给出峰值：

$$
c_{max}=1.36,
$$

稳态值：

$$
c(\infty)=1,
$$

以及峰值时间：

$$
t_p=0.5\ \mathrm{s}.
$$

这是标准二阶系统的反问题：先由超调量求阻尼比，再由峰值时间求自然频率。

### 第一步：由超调量求阻尼比

$$
M_p
=\frac{1.36-1}{1}
=0.36.
$$

标准二阶关系为：

$$
M_p
=e^{-\frac{\pi\zeta}{\sqrt{1-\zeta^2}}}.
$$

直接反解可写为：

$$
\boxed{
\zeta
=\frac{-\ln M_p}{\sqrt{\pi^2+(\ln M_p)^2}}
\approx0.309.
}
$$

这个反解式比查图更稳定，也便于计算器输入。

### 第二步：由峰值时间求自然频率

$$
t_p
=\frac{\pi}{\omega_n\sqrt{1-\zeta^2}}.
$$

所以：

$$
\boxed{
\omega_n
=\frac{\pi}{t_p\sqrt{1-\zeta^2}}
\approx6.62\ \mathrm{rad/s}.
}
$$

### 第三步：由闭环标准型反推开环

闭环传递函数为：

$$
T(s)
=\frac{\omega_n^2}{s^2+2\zeta\omega_ns+\omega_n^2}.
$$

单位负反馈下：

$$
G(s)=\frac{T(s)}{1-T(s)}.
$$

因此：

$$
\boxed{
G(s)
=\frac{\omega_n^2}{s(s+2\zeta\omega_n)}
\approx\frac{43.8}{s(s+4.09)}.
}
$$

### 第四步：调节时间必须声明误差带与公式约定

常用近似为：

$$
t_s\approx\frac{4}{\zeta\omega_n}
\qquad
\text{百分之二误差带},
$$

$$
t_s\approx\frac{3}{\zeta\omega_n}
\qquad
\text{百分之五误差带}.
$$

代入本题参数：

$$
t_{s,2\%}\approx1.95\ \mathrm{s},
$$

$$
t_{s,5\%}\approx1.47\ \mathrm{s}.
$$

第三方解析采用更保守的系数：

$$
4.4,
\qquad
3.5.
$$

不同教材可能得到约：

$$
2.15\ \mathrm{s},
\qquad
1.71\ \mathrm{s}.
$$

考场应按目标院校教材的定义作答，并在结果旁写明误差带。

### 易错点

- 百分之三十六应写成小数再代公式；
- 峰值时间使用阻尼振荡频率，不是直接使用自然频率；
- 从闭环反推开环时必须使用单位反馈关系，不能把闭环标准型直接当开环。

### 连接之前的知识点

- [经典控制：标准二阶系统](2.经典控制理论（公理化推导与简例）.md#242-标准二阶系统)
- [真题通法：频域与时域结果的验算思想](6.考研真题通法（从条件翻译到验算）.md#610-一页式考场处理流程)

---

## 7.4 第四题：含共轭复极点的根轨迹

### 识别入口

开环传递函数为：

$$
G(s)
=\frac{K^*}{s(s+4)(s+2-j4)(s+2+j4)}.
$$

题目要求绘制增益从零到无穷的根轨迹。真正的难点不是规则数量，而是复极点处的出射角和根轨迹穿越虚轴的位置。

### 第一步：先列出几何骨架

开环极点为：

$$
0,
\qquad
-4,
\qquad
-2+j4,
\qquad
-2-j4.
$$

没有有限零点，因此有四条分支趋向无穷远。

实轴根轨迹区段为：

$$
[-4,0].
$$

渐近线中心为：

$$
\sigma_a
=\frac{0-4-2-2}{4}
=-2.
$$

渐近线角度为：

$$
\varphi_a
=\frac{(2k+1)180^\circ}{4}.
$$

因此：

$$
\boxed{
\varphi_a
=45^\circ,
\ 135^\circ,
\ 225^\circ,
\ 315^\circ.
}
$$

### 第二步：分离点只接受合法候选

由特征方程：

$$
K^*
=-s(s+4)\left[(s+2)^2+16\right].
$$

求驻点：

$$
\frac{dK^*}{ds}=0.
$$

得到候选点：

$$
-2,
\qquad
-2+j\sqrt6,
\qquad
-2-j\sqrt6.
$$

其中实轴分离点为：

$$
\boxed{s=-2.}
$$

复数驻点可帮助理解轨迹几何，但不能套用“实轴分离点”语言。每个候选仍需满足角度条件和正增益条件。

### 第三步：求虚轴交点

闭环特征多项式为：

$$
D(s)
=s^4+8s^3+36s^2+80s+K^*.
$$

令：

$$
s=j\omega.
$$

分离实部和虚部：

$$
\omega^4-36\omega^2+K^*=0,
$$

$$
-8\omega^3+80\omega=0.
$$

除去原点解后：

$$
\omega=\sqrt{10}.
$$

代回得到：

$$
K^*=260.
$$

因此虚轴交点为：

$$
\boxed{s=\pm j\sqrt{10}.}
$$

### 第四步：复极点出射角

从上半平面极点出发，按角度条件减去其余极点对该点的角度贡献，可得：

$$
\theta_d=-90^\circ.
$$

由共轭对称性，下半平面极点的出射角为：

$$
\theta_d=90^\circ.
$$

### 可复用技巧

根轨迹草图至少包含六个锚点：

1. 开环极点与零点；
2. 实轴区段；
3. 渐近线中心；
4. 渐近线角度；
5. 分离点或会合点；
6. 虚轴交点和复极点出射角。

只画“形状像对称曲线”通常拿不到综合题的过程分。

### 连接之前的知识点

- [经典控制：根轨迹定义与规则](2.经典控制理论（公理化推导与简例）.md#27-根轨迹)
- [真题通法：分离点的重根条件与合法性筛选](6.考研真题通法（从条件翻译到验算）.md#63-根轨迹分离点与只有一个实极点用重根条件不靠图猜)

---

## 7.5 第五题：参考输入与扰动共同作用的稳态误差

### 识别入口

控制器为：

$$
C(s)=\frac{1}{0.1s+1}.
$$

对象为：

$$
P(s)=\frac{10}{s(s+4)}.
$$

参考输入和扰动分别为：

$$
r(t)=t,
$$

$$
n(t)=0.5\cdot1(t).
$$

扰动加在控制器之后、对象之前。此题不能把扰动直接塞进位置、速度、加速度误差系数表。

### 第一步：先判闭环稳定

闭环特征多项式为：

$$
(0.1s+1)s(s+4)+10.
$$

展开：

$$
0.1s^3+1.4s^2+4s+10.
$$

三阶 Hurwitz 条件成立：

$$
1.4\times4>0.1\times10.
$$

因此可以使用终值定理。

### 第二步：参考输入单独作用

令扰动为零。开环为：

$$
L(s)
=C(s)P(s)
=\frac{10}{(0.1s+1)s(s+4)}.
$$

速度误差系数为：

$$
K_v
=\lim_{s\to0}sL(s)
=2.5.
$$

单位斜坡误差为：

$$
e_{ss,r}
=\frac{1}{K_v}
=0.4.
$$

### 第三步：扰动单独作用

令参考输入为零。由：

$$
y=P(Ce+n),
$$

$$
e=-y,
$$

得到：

$$
\frac{E(s)}{N(s)}
=-\frac{P(s)}{1+C(s)P(s)}.
$$

化简：

$$
\frac{E(s)}{N(s)}
=-\frac{10(s+10)}{s^3+14s^2+40s+100}.
$$

对阶跃扰动：

$$
N(s)=\frac{0.5}{s}.
$$

终值为：

$$
e_{ss,n}
=\lim_{s\to0}
s\frac{E(s)}{N(s)}\frac{0.5}{s}
=-0.5.
$$

### 第四步：线性叠加

$$
\boxed{
e_{ss}
=e_{ss,r}+e_{ss,n}
=0.4-0.5
=-0.1.
}
$$

负号表示输出在稳态比参考量大，而不是误差“大小”小于零。

### 可复用技巧

多输入线性系统统一使用逐源置零：

1. 保留一个独立输入；
2. 其余独立输入设为零；
3. 求该输入到误差的传递函数；
4. 分别用终值定理；
5. 最后线性叠加。

### 连接之前的知识点

- [经典控制：误差传递函数与终值定理](2.经典控制理论（公理化推导与简例）.md#26-稳态误差)
- [真题通法：复合输入与扰动的逐源置零](6.考研真题通法（从条件翻译到验算）.md#62-复合输入稳态误差先判稳定再按输入阶次拆开)

---

## 7.6 第六题：由 Bode 渐近线反求开环传递函数

### 识别入口

题图给出斜率依次变化：

$$
-40,
\quad
-20,
\quad
-40,
\quad
-60
\quad
\mathrm{dB/dec}.
$$

转折频率为：

$$
0.8,
\qquad
30,
\qquad
50
\quad
\mathrm{rad/s}.
$$

开环增益为：

$$
k=3.2.
$$

### 斜率变化直接对应零极点

低频初始斜率为负四十，说明有两个积分环节：

$$
\frac{1}{s^2}.
$$

在第一处转折频率，斜率增加二十，说明出现一个零点：

$$
1+\frac{s}{0.8}.
$$

在后两处转折频率，斜率各减少二十，说明各出现一个极点：

$$
1+\frac{s}{30},
$$

$$
1+\frac{s}{50}.
$$

所以：

$$
\boxed{
G(s)
=\frac{
3.2\left(1+\frac{s}{0.8}\right)
}{
s^2\left(1+\frac{s}{30}\right)
\left(1+\frac{s}{50}\right)
}.
}
$$

### 穿越频率的快速估算

题图显示穿越频率处于：

$$
0.8\ll\omega_c\ll30.
$$

在这个频段可用渐近近似：

$$
\left|1+\frac{j\omega}{0.8}\right|
\approx\frac{\omega}{0.8},
$$

$$
\left|1+\frac{j\omega}{30}\right|
\approx1,
$$

$$
\left|1+\frac{j\omega}{50}\right|
\approx1.
$$

于是：

$$
|G(j\omega)|
\approx\frac{3.2(\omega/0.8)}{\omega^2}
=\frac4\omega.
$$

令幅值为一：

$$
\boxed{\omega_c\approx4\ \mathrm{rad/s}.}
$$

### 相频曲线必须回到精确因子

$$
\varphi(\omega)
=-180^\circ
+\arctan\frac{\omega}{0.8}
-\arctan\frac{\omega}{30}
-\arctan\frac{\omega}{50}.
$$

在穿越频率处：

$$
\varphi(4)\approx-113.5^\circ.
$$

对应相角裕度约为：

$$
\gamma\approx66.5^\circ.
$$

### 可复用技巧

斜率每增加二十分贝每十倍频，对应一个一阶零点；每减少二十分贝每十倍频，对应一个一阶极点。初始斜率先确定原点零极点数量，再按每个转折点补因子。

### 连接之前的知识点

- [经典控制：Bode 图的一阶因子](2.经典控制理论（公理化推导与简例）.md#282-一阶因子)
- [真题通法：穿越频率与相位裕度不能串频率](6.考研真题通法（从条件翻译到验算）.md#64-频率裕度先解幅值再在正确频率算相位)

---

## 7.7 第七题：开环不稳定、含右半平面零点的 Nyquist 题

### 识别入口

开环传递函数为：

$$
G(s)
=\frac{k(s-1)}{(s-2)(s-4)}.
$$

它有两个右半平面开环极点：

$$
s=2,
\qquad
s=4.
$$

所以：

$$
P=2.
$$

同时存在右半平面零点：

$$
s=1.
$$

系统是非最小相位系统。不能套“开环稳定时不包围负一即可”的简化结论。

### Nyquist 曲线的关键锚点

零频幅值为：

$$
G(j0)=-\frac{k}{8}.
$$

高频时：

$$
\lim_{\omega\to\infty}G(j\omega)=0.
$$

相位穿越条件可写为：

$$
\arctan\omega
=\arctan\frac{\omega}{4}
+\arctan\frac{\omega}{2}.
$$

解得：

$$
\omega=\sqrt2.
$$

此时实轴交点为：

$$
G(j\sqrt2)=-\frac{k}{6}.
$$

因此两个重要参数边界来自：

$$
-\frac{k}{6}=-1,
$$

以及：

$$
-\frac{k}{8}=-1.
$$

即：

$$
k=6,
\qquad
k=8.
$$

### 稳定范围

按 Nyquist 绕数与：

$$
P=2
$$

配合，闭环稳定范围为：

$$
\boxed{6<k<8.}
$$

### 用闭环特征方程独立验算

闭环特征方程为：

$$
(s-2)(s-4)+k(s-1)=0.
$$

展开：

$$
s^2+(k-6)s+(8-k)=0.
$$

二阶连续系统稳定要求两个非首项系数均为正：

$$
k-6>0,
$$

$$
8-k>0.
$$

仍得到：

$$
6<k<8.
$$

这不是用另一种方法替代 Nyquist，而是非常可靠的考场验算。

### 不稳定时右半平面闭环根个数

当：

$$
0<k<6
$$

时，闭环有两个实部为正的根。

当：

$$
k>8
$$

时，闭环有一个实部为正的根。

两个边界分别是临界稳定情形：

$$
k=6,
$$

$$
k=8.
$$

### 易错点

- 开环右半平面极点数不是零；
- 右半平面零点改变相位，但不计入 Nyquist 判据中的开环极点数；
- 必须事先声明顺时针或逆时针绕数的符号约定；
- 参数边界不能并入渐近稳定范围。

### 连接之前的知识点

- [经典控制：Nyquist 判据与符号约定](2.经典控制理论（公理化推导与简例）.md#29-nyquist-判据)
- [真题通法：稳定范围必须用闭环特征方程验算](6.考研真题通法（从条件翻译到验算）.md#610-一页式考场处理流程)

---

## 7.8 第八题：采样系统的脉冲传递函数与稳定范围

### 识别入口

连续对象为：

$$
G_0(s)=\frac{k}{s(s+1)}.
$$

采样周期为：

$$
T=1\ \mathrm{s}.
$$

题目要求脉冲传递函数、闭环特征方程和稳定增益范围。这是典型的“连续对象取脉冲传递函数，再在单位圆内判稳定”。题图只画出理想采样器，没有画零阶保持器。

### 第一步：由连续脉冲响应求脉冲传递函数

连续对象的脉冲响应为：

$$
g(t)
=\mathcal L^{-1}
\left\{
\frac{k}{s(s+1)}
\right\}
=k(1-e^{-t}).
$$

在采样时刻取值：

$$
g(nT)
=k(1-e^{-nT}).
$$

因此：

$$
G(z)
=\mathcal Z\{g(nT)\}.
$$

当采样周期为一秒时：

$$
G(z)
=k\left[
\frac{z}{z-1}
-\frac{z}{z-e^{-1}}
\right].
$$

得到：

$$
\boxed{
G(z)
=\frac{0.6321kz}{(z-1)(z-0.3679)}.
}
$$

如果题图另有零阶保持器，则应把保持器动态计入，所得脉冲传递函数一般不同。判断公式前必须先看清结构图。

### 第二步：写闭环特征方程

单位负反馈下：

$$
1+G(z)=0.
$$

因此：

$$
(z-1)(z-0.3679)+0.6321kz=0.
$$

展开：

$$
D(z)
=z^2+(0.6321k-1.3679)z+0.3679.
$$

### 第三步：用二阶 Jury 条件

对首一多项式：

$$
z^2+a_1z+a_0,
$$

单位圆稳定条件为：

$$
|a_0|<1,
$$

$$
1+a_1+a_0>0,
$$

$$
1-a_1+a_0>0.
$$

本题第一条自动满足。第二条给出：

$$
k>0.
$$

第三条给出：

$$
k<\frac{2(1+e^{-1})}{1-e^{-1}}
\approx4.328.
$$

所以：

$$
\boxed{0<k<4.328.}
$$

### 与双线性变换法的关系

也可以使用映射：

$$
z=\frac{1+w}{1-w}.
$$

它把单位圆内部映射到左半平面，再对变换后的多项式做劳斯。本题为二阶，Jury 条件更短；高阶题或题目明确要求变换法时，再使用映射。

### 易错点

- 不能把连续传递函数中的变量直接换成离散变量；
- 闭环稳定区域是单位圆内部，不是左半平面；
- 小数零点来自指数项，保留足够有效数字再算参数边界。

### 连接之前的知识点

- [现代控制：连续模型与离散模型的区别](4.现代控制理论（公理化推导与简例）.md#13-连续系统的零阶保持离散化)
- [真题通法：离散系统单位圆判据](6.考研真题通法（从条件翻译到验算）.md#67-离散系统稳定稳定区域从左半平面换成单位圆)

---

## 7.9 第九题：同一传递函数的可控标准型与对角型

### 识别入口

题目给出：

$$
G(s)
=\frac{s+2}{(s+1)(s+3)}
=\frac{s+2}{s^2+4s+3}.
$$

要求写出可控标准型和对角型，并画模拟结构图。核心不是背两套矩阵，而是理解“同一输入输出关系可以有不同状态坐标”。

### 可控标准型

对分母：

$$
s^2+a_1s+a_0
$$

和分子：

$$
b_1s+b_0,
$$

本题系数为：

$$
a_1=4,
\qquad
a_0=3,
$$

$$
b_1=1,
\qquad
b_0=2.
$$

一种可控标准型为：

$$
\boxed{
A_c=
\begin{bmatrix}
0&1\\
-3&-4
\end{bmatrix},
\qquad
B_c=
\begin{bmatrix}
0\\
1
\end{bmatrix},
}
$$

$$
\boxed{
C_c=
\begin{bmatrix}
2&1
\end{bmatrix},
\qquad
D=0.
}
$$

验算：

$$
C_c(sI-A_c)^{-1}B_c
=\frac{s+2}{s^2+4s+3}.
$$

对应模拟结构图的识别方法是：两个积分器串联，最后一个状态导数由输入和状态反馈组合产生，输出是两个状态的线性组合。

### 对角型

先作部分分式展开：

$$
G(s)
=\frac{0.5}{s+1}
+\frac{0.5}{s+3}.
$$

每个一阶模态对应一个状态，因此可取：

$$
\boxed{
A_d=
\begin{bmatrix}
-1&0\\
0&-3
\end{bmatrix},
\qquad
B_d=
\begin{bmatrix}
1\\
1
\end{bmatrix},
}
$$

$$
\boxed{
C_d=
\begin{bmatrix}
0.5&0.5
\end{bmatrix},
\qquad
D=0.
}
$$

验算：

$$
C_d(sI-A_d)^{-1}B_d
=\frac{0.5}{s+1}
+\frac{0.5}{s+3}.
$$

### 可复用技巧

- 可控标准型：分母系数进入状态矩阵末行，分子系数进入输出矩阵；
- 对角型：极点进入状态矩阵对角线，留数分配到输入和输出矩阵；
- 状态实现不唯一，必须用传递函数公式回验。

### 易错点

- 分母末行系数带负号；
- 输出矩阵的分子系数顺序与选用的标准型约定有关；
- 对角型只有在能够对角化时直接成立；重根或 Jordan 块需要另行处理。

### 连接之前的知识点

- [现代控制：传递函数分子与输出矩阵](4.现代控制理论（公理化推导与简例）.md#15-传递函数分子与-c-矩阵)
- [现代控制：状态空间到传递函数](4.现代控制理论（公理化推导与简例）.md#32-推论-1状态空间到传递函数)
- [现代控制速查：建模](5.现代控制知识框架速查.md#51-建模)

---

## 7.10 第十题：电路状态建模与能观性判别

### 识别入口

题目指定两个电感支路电流为状态：

$$
x=
\begin{bmatrix}
i_1\\
i_2
\end{bmatrix}.
$$

输入为电压：

$$
u_i.
$$

输出为右侧电阻两端电压：

$$
u_o.
$$

此题分成两层：先按 KVL 建立状态方程，再用能观矩阵判断输出是否足以重建两个状态。

### 第一步：按统一回路方向写 KVL

由外侧回路：

$$
u_i
=Ri_1
+L\frac{di_1}{dt}
+R_0(i_1-i_2).
$$

由内侧回路：

$$
Ri_2
+L\frac{di_2}{dt}
=R_0(i_1-i_2).
$$

输出为：

$$
u_o=R_0(i_1-i_2).
$$

整理得：

$$
\dot x=Ax+Bu,
\qquad
y=Cx,
$$

其中：

$$
\boxed{
A=
\begin{bmatrix}
-\dfrac{R+R_0}{L}&\dfrac{R_0}{L}\\
\dfrac{R_0}{L}&-\dfrac{R+R_0}{L}
\end{bmatrix},
}
$$

$$
\boxed{
B=
\begin{bmatrix}
\dfrac1L\\
0
\end{bmatrix},
\qquad
C=
\begin{bmatrix}
R_0&-R_0
\end{bmatrix}.
}
$$

### 第二步：构造能观矩阵

$$
\mathcal O
=
\begin{bmatrix}
C\\
CA
\end{bmatrix}.
$$

计算：

$$
CA
=-rac{R+2R_0}{L}C.
$$

因此：

$$
\mathcal O
=
\begin{bmatrix}
C\\
-\dfrac{R+2R_0}{L}C
\end{bmatrix}.
$$

第二行始终是第一行的倍数。对正常的正值电阻参数：

$$
\boxed{
\operatorname{rank}(\mathcal O)=1<2.
}
$$

按照卷面所画的对称参数和所指定输出，系统不完全能观。

若退化地取零输出电阻，则能观矩阵秩进一步降为零，结论仍是不完全能观。

### 第三方解析中的错误

第三方答案把能观矩阵第二行的第二个元素展开错了，进而得到“某两个电阻不相等时可观”的结论。按它自己前面列出的状态矩阵重新相乘，应有：

$$
CA
=-rac{R+2R_0}{L}
\begin{bmatrix}
R_0&-R_0
\end{bmatrix}.
$$

因此秩不会因为改变两个正电阻的数值而升到二。

如果原始试题实际存在扫描不清的不同元件参数，例如两个支路电阻并不相同，则必须重新以不同参数写状态矩阵；不能沿用对称状态矩阵后再凭结论修改秩。

### 物理解释

输出只测量电流差：

$$
i_1-i_2.
$$

共同变化模态：

$$
i_1+i_2
$$

不会直接出现在输出中。由于状态矩阵对称，这两个模态彼此不混合，所以共同模态始终无法从输出恢复。

这个物理解释比只算一个二阶行列式更可靠，也能帮助发现第三方解析的代数错误。

### 可复用技巧

1. 先按元件方向写 KVL，不要从答案矩阵反推；
2. 检查状态矩阵量纲；
3. 构造能观矩阵并求秩；
4. 再观察输出是否只测到某个对称或差分模态；
5. 代数秩与物理解释应一致。

### 连接之前的知识点

- [现代控制：从微分方程建立状态模型](4.现代控制理论（公理化推导与简例）.md#1-从微分方程建立状态模型)
- [现代控制：能观矩阵与秩判据](4.现代控制理论（公理化推导与简例）.md#7-能观性)
- [现代控制速查：结构性质](5.现代控制知识框架速查.md#54-结构性质)

---

## 7.11 十道题之间的知识连接

| 题号 | 表面题型 | 底层对象 | 最重要的迁移能力 |
|---|---|---|---|
| 1 | RC 电路 | 节点方程与传递函数 | 跨接网络优先列 KCL |
| 2 | 微分方程 | 零输入与零状态响应 | 非零初值不能套零初值传递函数 |
| 3 | 二阶曲线 | 阻尼比与自然频率 | 从图反求参数并声明误差带 |
| 4 | 根轨迹 | 闭环重根与角度条件 | 候选点必须筛选并求虚轴交点 |
| 5 | 稳态误差 | 多输入误差传递函数 | 逐源置零后叠加 |
| 6 | Bode 反推 | 零极点与斜率 | 先看斜率变化，再写标准因子 |
| 7 | Nyquist | 绕数与右半平面极点 | 不能使用开环稳定的简化结论 |
| 8 | 采样系统 | 脉冲传递函数与单位圆 | 先离散，再做 Jury 判据 |
| 9 | 状态实现 | 坐标选择 | 用输入输出关系回验不同实现 |
| 10 | 能观性 | 输出可恢复的状态方向 | 秩计算必须与物理模态一致 |

建议复习顺序不是按题号，而是按主线：

$$
\text{建模}
\longrightarrow
\text{时域}
\longrightarrow
\text{稳定性}
\longrightarrow
\text{根轨迹与频域}
\longrightarrow
\text{离散系统}
\longrightarrow
\text{状态空间}.
$$
