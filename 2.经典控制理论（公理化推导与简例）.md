# 2. 经典控制理论：公理化推导与简例

## 2.0 目录

1. [基本公设与对象](#21-基本公设与对象)
2. [传递函数](#22-传递函数)
3. [反馈闭环](#23-反馈闭环)
4. [时域响应](#24-时域响应)
5. [稳定性与劳斯判据](#25-稳定性与劳斯判据)
6. [稳态误差](#26-稳态误差)
7. [根轨迹](#27-根轨迹)
8. [频率响应与 Bode 图](#28-频率响应与-bode-图)
9. [Nyquist 判据](#29-nyquist-判据)
10. [稳定裕度](#210-稳定裕度)
11. [PID 控制](#211-pid-控制)
12. [超前与滞后校正](#212-超前与滞后校正)
13. [经典控制与现代控制的边界](#213-经典控制与现代控制的边界)
14. [计算检查表](#214-计算检查表)

## 2.1 基本公设与对象

**公设 C1（线性定常）**　系统在研究范围内满足线性与时不变性。

**公设 C2（因果性）**　输出只由当前及过去的输入决定。

**公设 C3（零初值传递函数）**　传递函数只描述零初值下的输入输出关系。

**公设 C4（负反馈）**　除非特别说明，闭环采用负反馈。

**公设 C5（正弦稳态）**　稳定线性定常系统受正弦输入时，稳态输出频率不变，只改变幅值与相位。

经典控制主要研究单输入单输出系统：

$$
u(t)\longrightarrow G(s)\longrightarrow y(t).
$$

## 2.2 传递函数

### 2.2.1 定义

零初值下，传递函数定义为

$$
G(s)=\frac{Y(s)}{U(s)}.
$$

### 2.2.2 从微分方程推出传递函数

给定

$$
\ddot y+3\dot y+2y=u.
$$

零初值拉普拉斯变换得到

$$
s^2Y(s)+3sY(s)+2Y(s)=U(s).
$$

提取输出：

$$
\left(s^2+3s+2\right)Y(s)=U(s).
$$

因此

$$
\boxed{G(s)=\frac{1}{s^2+3s+2}.}
$$

### 2.2.3 零点、极点与增益

一般有

$$
G(s)=K\frac{\prod_i(s-z_i)}{\prod_j(s-p_j)}.
$$

分子根是零点，分母根是极点。极点决定自然模态，零点改变响应形状。

**简例**

$$
G(s)=\frac{s+1}{(s+2)(s+3)}.
$$

零点为负一，极点为负二和负三。

## 2.3 反馈闭环

### 2.3.1 闭环传递函数

设前向通道为 G，反馈通道为 H。负反馈关系为

$$
E(s)=R(s)-H(s)Y(s),
$$

$$
Y(s)=G(s)E(s).
$$

代入并整理：

$$
Y=G(R-HY),
$$

$$
\left(1+GH\right)Y=GR.
$$

所以

$$
\boxed{\frac{Y(s)}{R(s)}=\frac{G(s)}{1+G(s)H(s)}.}
$$

单位反馈时

$$
H(s)=1.
$$

### 2.3.2 特征方程

闭环极点由下式决定：

$$
\boxed{1+G(s)H(s)=0.}
$$

**简例**　取

$$
G(s)=\frac{K}{s+1},
\qquad H(s)=1.
$$

则

$$
\frac{Y}{R}=\frac{K}{s+1+K},
$$

闭环极点为

$$
s=-(1+K).
$$

当增益为正时，闭环稳定且增益越大响应越快。

## 2.4 时域响应

### 2.4.1 一阶系统

标准一阶系统为

$$
G(s)=\frac{K}{Ts+1}.
$$

单位阶跃输入满足

$$
R(s)=\frac{1}{s}.
$$

于是

$$
Y(s)=\frac{K}{s(Ts+1)}.
$$

部分分式展开并反变换：

$$
\boxed{y(t)=K\left(1-e^{-t/T}\right).}
$$

在一个时间常数后，输出达到终值的约百分之六十三点二。

### 2.4.2 标准二阶系统

$$
G(s)=\frac{\omega_n^2}{s^2+2\zeta\omega_ns+\omega_n^2}.
$$

其中阻尼比决定振荡程度，自然频率决定时间尺度。

欠阻尼时

$$
0<\zeta<1.
$$

阻尼振荡频率为

$$
\omega_d=\omega_n\sqrt{1-\zeta^2}.
$$

峰值超调量为

$$
\boxed{M_p=e^{-\frac{\pi\zeta}{\sqrt{1-\zeta^2}}}.}
$$

百分之二误差带的调节时间近似为

$$
\boxed{t_s\approx\frac{4}{\zeta\omega_n}.}
$$

**简例**　取

$$
\zeta=0.5,
\qquad \omega_n=4.
$$

则

$$
M_p\approx0.163,
$$

$$
t_s\approx2\ \mathrm{s}.
$$

## 2.5 稳定性与劳斯判据

### 2.5.1 稳定性定理

有理连续系统 BIBO 稳定，当且仅当所有闭环极点都在开左半平面：

$$
\mathrm{Re}(p_i)<0.
$$

### 2.5.2 劳斯判据

设闭环特征多项式为

$$
a_ns^n+a_{n-1}s^{n-1}+\cdots+a_1s+a_0=0.
$$

构造劳斯表后，第一列符号变化次数等于右半平面根的个数。系统稳定当且仅当第一列元素同号且非零。

### 2.5.3 三阶系统简例

设特征多项式为

$$
s^3+2s^2+3s+K.
$$

劳斯表第一列为

$$
1,
\qquad 2,
\qquad \frac{6-K}{2},
\qquad K.
$$

全部为正要求

$$
\boxed{0<K<6.}
$$

## 2.6 稳态误差

### 2.6.1 误差传递函数

单位反馈下

$$
E(s)=\frac{R(s)}{1+G(s)}.
$$

若闭环稳定，可用终值定理：

$$
e_{ss}=\lim_{t\to\infty}e(t)
=\lim_{s\to0}sE(s).
$$

### 2.6.2 静态误差系数

位置、速度和加速度误差系数分别为

$$
K_p=\lim_{s\to0}G(s),
$$

$$
K_v=\lim_{s\to0}sG(s),
$$

$$
K_a=\lim_{s\to0}s^2G(s).
$$

对应的稳态误差为

$$
e_{ss,\mathrm{step}}=\frac{1}{1+K_p},
$$

$$
e_{ss,\mathrm{ramp}}=\frac{1}{K_v},
$$

$$
e_{ss,\mathrm{parabolic}}=\frac{1}{K_a}.
$$

**简例**　单位反馈系统取

$$
G(s)=\frac{10}{s(s+2)}.
$$

系统含一个积分环节，因此为一型系统。其速度误差系数为

$$
K_v=\lim_{s\to0}\frac{10}{s+2}=5.
$$

单位斜坡稳态误差为

$$
e_{ss}=\frac{1}{5}=0.2.
$$

## 2.7 根轨迹

### 2.7.1 定义

根轨迹是增益从零变化到无穷时，闭环极点在复平面上的轨迹。

设开环为

$$
L(s)=KG_0(s).
$$

闭环特征方程为

$$
1+KG_0(s)=0.
$$

因此轨迹点满足角度条件

$$
\boxed{\angle G_0(s)=(2k+1)\pi,}
$$

以及幅值条件

$$
\boxed{K=\frac{1}{\left|G_0(s)\right|}.}
$$

### 2.7.2 基本规则

1. 分支从开环极点出发；
2. 分支终止于开环零点或无穷远；
3. 实轴上一点右侧的实极点与实零点总数为奇数时，该点在根轨迹上；
4. 根轨迹关于实轴对称。

### 2.7.3 简例

取

$$
G_0(s)=\frac{1}{s(s+2)}.
$$

闭环特征方程为

$$
s^2+2s+K=0.
$$

闭环极点为

$$
s=-1\pm\sqrt{1-K}.
$$

当增益小于一时，两根在实轴；增益大于一时，两根从负一处分离并沿竖直方向移动。

## 2.8 频率响应与 Bode 图

### 2.8.1 频率响应

令

$$
s=j\omega,
$$

得到

$$
G(j\omega)=\left|G(j\omega)\right|e^{j\phi(\omega)}.
$$

幅频特性为

$$
20\log_{10}\left|G(j\omega)\right|,
$$

相频特性为

$$
\phi(\omega)=\angle G(j\omega).
$$

### 2.8.2 一阶因子

对一阶惯性环节

$$
G(s)=\frac{1}{1+sT},
$$

有

$$
\left|G(j\omega)\right|
=\frac{1}{\sqrt{1+(\omega T)^2}},
$$

$$
\phi(\omega)=-\arctan(\omega T).
$$

转折频率为

$$
\omega_b=\frac{1}{T}.
$$

转折频率后，渐近幅频曲线斜率减少每十倍频二十分贝。

## 2.9 Nyquist 判据

设开环传递函数为 L，并采用“顺时针绕负一圈数为正”的约定。记：

- P 为开环右半平面极点数；
- N 为 Nyquist 曲线顺时针包围负一的净圈数；
- Z 为闭环右半平面极点数。

则

$$
\boxed{N=Z-P.}
$$

闭环稳定要求

$$
Z=0.
$$

因此必须满足

$$
N=-P.
$$

**简例**　若开环稳定，则

$$
P=0.
$$

此时 Nyquist 曲线不包围负一即可保证闭环稳定。

## 2.10 稳定裕度

### 2.10.1 相位裕度

增益交叉频率满足

$$
\left|L(j\omega_{gc})\right|=1.
$$

相位裕度为

$$
\boxed{\mathrm{PM}=180^\circ+\angle L(j\omega_{gc}).}
$$

### 2.10.2 增益裕度

相位交叉频率满足

$$
\angle L(j\omega_{pc})=-180^\circ.
$$

增益裕度为

$$
\boxed{\mathrm{GM}=\frac{1}{\left|L(j\omega_{pc})\right|}.}
$$

分贝形式为

$$
\mathrm{GM}_{\mathrm{dB}}
=-20\log_{10}\left|L(j\omega_{pc})\right|.
$$

## 2.11 PID 控制

PID 控制器为

$$
\boxed{C(s)=K_p+\frac{K_i}{s}+K_ds.}
$$

时域形式为

$$
u(t)=K_pe(t)+K_i\int_0^t e(\tau)\,d\tau+K_d\frac{de(t)}{dt}.
$$

三项作用：

1. 比例项提高响应速度，但过大可能增加振荡；
2. 积分项消除恒值稳态误差，但可能降低稳定裕度；
3. 微分项预测误差变化，可增加阻尼，但会放大高频噪声。

实际微分通常加入滤波：

$$
K_d\frac{Ns}{s+N}.
$$

## 2.12 超前与滞后校正

### 2.12.1 校正问题的数学目标

设被控对象为

$$
G(s),
$$

串联校正器为

$$
C(s).
$$

校正后的开环传递函数为

$$
L_c(s)=C(s)G(s).
$$

闭环传递函数为

$$
\boxed{T_c(s)=\frac{C(s)G(s)}{1+C(s)G(s)H(s)}.}
$$

设计 C 的常见目标是：

1. 调整闭环极点与瞬态响应；
2. 增加相位裕度或增益裕度；
3. 提高低频增益，减小稳态误差；
4. 限制高频增益，避免放大噪声。

### 2.12.2 超前校正器的零极点

标准超前校正器为

$$
\boxed{
C_{lead}(s)=K\frac{1+Ts}{1+\alpha Ts},
\qquad 0<\alpha<1.
}
$$

零点为

$$
z=-\frac{1}{T},
$$

极点为

$$
p=-\frac{1}{\alpha T}.
$$

因为

$$
0<\alpha<1,
$$

所以

$$
\left|p\right|>\left|z\right|.
$$

即极点比零点更靠左。零点先提供正相位，较远的极点再把高频增益限制住。

### 2.12.3 超前校正的幅值与相位

令

$$
s=j\omega.
$$

忽略常数增益 K 后，幅值为

$$
\left|\frac{1+j\omega T}{1+j\alpha\omega T}\right|
=\sqrt{\frac{1+(\omega T)^2}{1+(\alpha\omega T)^2}}.
$$

相位为

$$
\phi(\omega)
=\arctan(\omega T)-\arctan(\alpha\omega T).
$$

对频率求导：

$$
\frac{d\phi}{d\omega}
=\frac{T}{1+(\omega T)^2}
-\frac{\alpha T}{1+(\alpha\omega T)^2}.
$$

令导数为零，得到最大相位对应频率：

$$
\boxed{\omega_m=\frac{1}{T\sqrt{\alpha}}.}
$$

代回相位式可得

$$
\boxed{
\sin\phi_{max}=\frac{1-\alpha}{1+\alpha}.
}
$$

因此给定所需最大超前角，可以反求

$$
\boxed{
\alpha=\frac{1-\sin\phi_{max}}{1+\sin\phi_{max}}.
}
$$

在最大相位频率处，幅值为

$$
\left|\frac{C_{lead}(j\omega_m)}{K}\right|
=\frac{1}{\sqrt{\alpha}}.
$$

对应的分贝提升为

$$
\Delta L_m=-10\log_{10}\alpha.
$$

### 2.12.4 超前校正的频域设计步骤

1. 由未校正 Bode 图求当前相位裕度；
2. 计算应补相角，并留出五到十二度余量；
3. 由所需相角求出参数 α；
4. 选择新的交叉频率，使对象幅值满足

   $$
   \left|KG(j\omega_m)\right|=\sqrt{\alpha}.
   $$

5. 由下式求 T：

   $$
   T=\frac{1}{\omega_m\sqrt{\alpha}}.
   $$

6. 重新计算交叉频率、相位裕度与闭环响应。

### 2.12.5 超前校正数值例子

单位反馈对象为

$$
G(s)=\frac{1}{s(1+s)}.
$$

未校正增益交叉频率由

$$
\left|G(j\omega)\right|
=\frac{1}{\omega\sqrt{1+\omega^2}}=1
$$

确定。令

$$
q=\omega^2,
$$

则

$$
q(1+q)=1.
$$

所以

$$
q=\frac{\sqrt{5}-1}{2},
$$

$$
\omega_{gc}\approx0.786\ \mathrm{rad/s}.
$$

此时相位为

$$
\angle G(j\omega_{gc})
=-90^\circ-\arctan(0.786)
\approx-128.2^\circ.
$$

原相位裕度为

$$
\mathrm{PM}_0\approx51.8^\circ.
$$

要求相位裕度约为六十度，并取五度余量，则所需超前角近似为

$$
\phi_{max}=60^\circ-51.8^\circ+5^\circ
=13.2^\circ.
$$

于是

$$
\alpha
=\frac{1-\sin13.2^\circ}{1+\sin13.2^\circ}
\approx0.629.
$$

新交叉频率应满足

$$
\left|G(j\omega_m)\right|=\sqrt{0.629}\approx0.793.
$$

求得

$$
\omega_m\approx0.926\ \mathrm{rad/s}.
$$

因此

$$
T=\frac{1}{0.926\sqrt{0.629}}
\approx1.36.
$$

校正器为

$$
\boxed{
C_{lead}(s)=\frac{1+1.36s}{1+0.855s}.
}
$$

其零点与极点近似为

$$
z=-0.735,
$$

$$
p=-1.17.
$$

在新交叉频率处，对象相位约为

$$
-90^\circ-\arctan(0.926)
\approx-132.8^\circ.
$$

加入约十三点二度的超前相位后，新的相位裕度约为

$$
\mathrm{PM}\approx60.4^\circ.
$$

### 2.12.6 滞后校正器的零极点

标准滞后校正器为

$$
\boxed{
C_{lag}(s)=K\frac{1+Ts}{1+\beta Ts},
\qquad \beta>1.
}
$$

零点为

$$
z=-\frac{1}{T},
$$

极点为

$$
p=-\frac{1}{\beta T}.
$$

因为

$$
\beta>1,
$$

所以极点比零点更靠近原点。

幅值为

$$
\left|\frac{C_{lag}(j\omega)}{K}\right|
=\sqrt{\frac{1+(\omega T)^2}{1+(\beta\omega T)^2}}.
$$

相位为

$$
\phi_{lag}(\omega)
=\arctan(\omega T)-\arctan(\beta\omega T).
$$

该相位为负，因此零点与极点通常放在交叉频率以下至少一个数量级，使新增负相位较小。

若取

$$
K=\beta,
$$

则低频增益为

$$
C_{lag}(0)=\beta,
$$

而高频增益趋于一：

$$
\lim_{\omega\to\infty}
\left|C_{lag}(j\omega)\right|=1.
$$

所以可以提高低频精度，同时尽量保持原交叉频率。

### 2.12.7 滞后校正数值例子

仍取单位反馈对象

$$
G(s)=\frac{1}{s(1+s)}.
$$

原速度误差系数为

$$
K_v=\lim_{s\to0}sG(s)=1.
$$

所以单位斜坡稳态误差为

$$
e_{ss}=1.
$$

若要求

$$
e_{ss}=0.1,
$$

则需要把低频增益提高十倍：

$$
\beta=10.
$$

原交叉频率约为

$$
\omega_c=0.786\ \mathrm{rad/s}.
$$

把滞后零点放在交叉频率的十分之一：

$$
\omega_z=0.0786\ \mathrm{rad/s}.
$$

因此

$$
T=\frac{1}{\omega_z}\approx12.72.
$$

滞后极点为

$$
\omega_p=\frac{\omega_z}{\beta}
=0.00786\ \mathrm{rad/s}.
$$

校正器取

$$
\boxed{
C_{lag}(s)
=10\frac{1+12.72s}{1+127.2s}.
}
$$

校正后的速度误差系数为

$$
K_v^{new}=10.
$$

于是

$$
e_{ss}^{new}=\frac{1}{10}=0.1.
$$

在原交叉频率附近，附加相位约为

$$
\arctan(10)-\arctan(100)
\approx-5.1^\circ.
$$

因此低频精度提高，而交叉频率与相位裕度只受到较小影响。

### 2.12.8 超前—滞后组合校正

当系统同时需要提高相位裕度和稳态精度时，可串联两种网络：

$$
C(s)=C_{lead}(s)C_{lag}(s).
$$

通常先设计超前部分确定交叉频率与相位裕度，再把滞后网络的零极点放在该交叉频率以下。

### 2.12.9 公式选择表

需要提高相位裕度或加快响应时，使用超前校正：

$$
0<\alpha<1.
$$

需要提高低频增益或减小稳态误差时，使用滞后校正：

$$
\beta>1.
$$

两项都需要时，使用超前—滞后组合校正。

### 2.12.10 术语区别

超前校正属于频域动态校正；现代控制中的参考前馈系数 N 属于静态参考增益校准。两者不是同一概念。

## 2.13 经典控制与现代控制的边界

经典控制关注输入输出关系：

$$
G(s)=\frac{Y(s)}{U(s)}.
$$

现代控制关注内部状态：

$$
\dot x=Ax+Bu,
\qquad y=Cx+Du.
$$

零初值下，两者由下式连接：

$$
G(s)=C(sI-A)^{-1}B+D.
$$

经典控制适合单输入单输出、频域整形与工程校正；现代控制适合多变量系统、状态反馈、观测器与最优控制。

## 2.14 计算检查表

### 2.14.1 建模

1. 写微分方程；
2. 在零初值下求传递函数；
3. 标出极点、零点和系统型别。

### 2.14.2 分析

1. 特征方程：

   $$
   1+G(s)H(s)=0.
   $$

2. 稳定性：极点或劳斯表；
3. 瞬态性能：阻尼比、自然频率、超调量、调节时间；
4. 稳态性能：终值定理与误差系数；
5. 相对稳定性：相位裕度与增益裕度。

### 2.14.3 设计

1. 根轨迹用于按增益配置极点；
2. Bode 图用于频域整形；
3. PID 用于误差反馈；
4. 超前校正改善相位与速度；
5. 滞后校正改善低频精度。
