---
title: 4. 现代控制理论：公理化推导与简例
subtitle: 从状态方程到前馈校正、观测器、LQR 与离散化
author: 整理版
date: 2026-09-01
lang: zh-CN
toc-title: 目录
---

# 目录

1. [记号、对象与五条公设](#0-记号对象与五条公设)
2. [从微分方程建立状态模型](#1-从微分方程建立状态模型)
3. [状态转移矩阵与状态响应](#2-状态转移矩阵与状态响应)
4. [拉普拉斯域、传递函数与极点](#3-拉普拉斯域传递函数与极点)
5. [连续系统稳定性](#4-连续系统稳定性)
6. [能控性](#5-能控性)
7. [状态反馈与极点配置](#6-状态反馈与极点配置)
8. [能观性](#7-能观性)
9. [全维状态观测器](#8-全维状态观测器)
10. [分离原理](#9-分离原理)
11. [参考前馈校正（预补偿）](#10-参考前馈校正预补偿)
12. [李雅普诺夫稳定性](#11-李雅普诺夫稳定性)
13. [线性二次型最优控制（LQR）](#12-线性二次型最优控制lqr)
14. [连续系统的零阶保持离散化](#13-连续系统的零阶保持离散化)
15. [一条完整推理链](#14-一条完整推理链)
16. [计算检查表](#15-计算检查表)

# 0. 记号、对象与五条公设

## 0.1 记号

连续线性定常系统写为

$$
\dot x=Ax+Bu,\qquad y=Cx+Du,
$$

其中

$$
x\in\mathbb{R}^n
$$

$$
u\in\mathbb{R}^m
$$

$$
y\in\mathbb{R}^p
$$

，故

$$
A\in\mathbb{R}^{n\times n},\quad B\in\mathbb{R}^{n\times m},\quad
C\in\mathbb{R}^{p\times n},\quad D\in\mathbb{R}^{p\times m}.
$$

## 0.2 公设

**公设 A1（状态模型）**　在所研究范围内，系统由有限维线性定常状态方程描述。

**公设 A2（线性代数）**　矩阵乘法满足结合律与分配律；满秩表示相应线性映射无方向丢失。

**公设 A3（矩阵指数）**

$$
e^{At}:=I+At+\frac{A^2t^2}{2!}+\cdots,
$$

且

$$
\frac{d}{dt}e^{At}=Ae^{At}
$$

$$
e^{A0}=I
$$

$$
e^{At}e^{A\tau}=e^{A(t+\tau)}
$$

**公设 A4（Cayley--Hamilton）**　A 满足自身特征多项式，因此

$$
A^n
$$

及更高次幂均可由

$$
I,A,\ldots,A^{n-1}
$$

线性表示。

**公设 A5（因果与已知量）**　初值、输入及模型参数一经指定，状态解唯一；讨论传递函数时取零初值。

> 本文所有结论都从 A1--A5 与明确列出的附加条件推出。

# 1. 从微分方程建立状态模型

## 1.1 定义 1：状态

状态

$$
x(t)
$$

是能够与未来输入一起唯一决定系统未来行为的最小变量组。

## 1.2 命题 1：二阶微分方程的状态实现

给定

$$
\ddot y+3\dot y+2y=u,
$$

取

$$
x_1=y
$$

$$
x_2=\dot y
$$

，则

$$
\dot x=
\begin{bmatrix}0&1\\-2&-3\end{bmatrix}x+
\begin{bmatrix}0\\1\end{bmatrix}u,
\qquad
y=\begin{bmatrix}1&0\end{bmatrix}x.
$$

**证明**　由定义直接得

$$
\dot x_1=x_2
$$

；原方程给出
$$
\dot x_2=\ddot y=-2x_1-3x_2+u
$$

。合并两式即得。证毕。

**例 1**　若

$$
x(0)=[1,0]^T
$$

，则初始位置为 1、初始速度为 0。矩阵 A 描述内部耦合，B 描述输入进入的方向，C 描述可测量的组合。

## 1.3 一般 n 阶微分方程为什么产生伴随矩阵

考虑不含输入导数的 n 阶微分方程：

$$
y^{(n)}+a_{n-1}y^{(n-1)}+\cdots+a_1\dot y+a_0y=bu.
$$

按“每降低一次导数就定义一个状态”取

$$
x_1=y,
$$

$$
x_2=\dot y,
$$

$$
x_3=\ddot y,
$$

$$
\vdots
$$

$$
x_n=y^{(n-1)}.
$$

前 n 减一行只是在传递导数：

$$
\dot x_1=x_2,
$$

$$
\dot x_2=x_3,
$$

$$
\vdots
$$

$$
\dot x_{n-1}=x_n.
$$

因此这些行的系数只能是“当前位置为零、后一状态位置为一”：

$$
\begin{bmatrix}
0&1&0&\cdots&0\\
0&0&1&\cdots&0\\
\vdots&\vdots&\vdots&\ddots&\vdots\\
0&0&0&\cdots&1
\end{bmatrix}.
$$

最后一行由原微分方程移项得到：

$$
y^{(n)}=-a_0y-a_1\dot y-\cdots-a_{n-1}y^{(n-1)}+bu.
$$

换成状态变量：

$$
\dot x_n=-a_0x_1-a_1x_2-\cdots-a_{n-1}x_n+bu.
$$

所以

$$
\boxed{
A=
\begin{bmatrix}
0&1&0&\cdots&0\\
0&0&1&\cdots&0\\
\vdots&\vdots&\vdots&\ddots&\vdots\\
0&0&0&\cdots&1\\
-a_0&-a_1&-a_2&\cdots&-a_{n-1}
\end{bmatrix}.
}
$$

末行出现负系数，是因为原方程中的各项被移到等号右侧。

输入只直接出现在最高阶导数方程中，因此

$$
\boxed{
B=
\begin{bmatrix}
0\\0\\\vdots\\0\\b
\end{bmatrix}.
}
$$

如果右端就是 u，则

$$
b=1.
$$

若输出就是原变量 y，则

$$
\boxed{C=\begin{bmatrix}1&0&\cdots&0\end{bmatrix},\qquad D=0.}
$$

这就是 A 的上方出现零和一、末行出现负系数、B 只有末项非零的原因。

## 1.4 二阶通式与物理例子

一般二阶方程为

$$
\ddot y+a_1\dot y+a_0y=bu.
$$

取

$$
x_1=y,
\qquad x_2=\dot y,
$$

得到

$$
\boxed{
A=\begin{bmatrix}0&1\\-a_0&-a_1\end{bmatrix},
\qquad
B=\begin{bmatrix}0\\b\end{bmatrix}.
}
$$

质量—弹簧—阻尼系统满足

$$
m\ddot y+c\dot y+ky=u.
$$

先除以质量：

$$
\ddot y+\frac{c}{m}\dot y+\frac{k}{m}y=\frac{1}{m}u.
$$

因此

$$
\boxed{
A=\begin{bmatrix}
0&1\\
-\dfrac{k}{m}&-\dfrac{c}{m}
\end{bmatrix},
\qquad
B=\begin{bmatrix}0\\\dfrac{1}{m}\end{bmatrix}.
}
$$

**数值例**　取

$$
m=2,
\qquad c=6,
\qquad k=4.
$$

则

$$
2\ddot y+6\dot y+4y=u,
$$

$$
\ddot y+3\dot y+2y=\frac{1}{2}u.
$$

所以

$$
A=\begin{bmatrix}0&1\\-2&-3\end{bmatrix},
\qquad
B=\begin{bmatrix}0\\\dfrac{1}{2}\end{bmatrix}.
$$

这里 B 的末项不是一，而是输入在最高阶方程中的实际系数。

## 1.5 传递函数分子与 C 矩阵

给定严格真二阶传递函数

$$
G(s)=\frac{b_1s+b_0}{s^2+a_1s+a_0},
$$

一种可控标准型实现为

$$
A=\begin{bmatrix}0&1\\-a_0&-a_1\end{bmatrix},
\qquad
B=\begin{bmatrix}0\\1\end{bmatrix},
$$

$$
\boxed{C=\begin{bmatrix}b_0&b_1\end{bmatrix},\qquad D=0.}
$$

验证：

$$
sI-A=
\begin{bmatrix}
s&-1\\
a_0&s+a_1
\end{bmatrix},
$$

$$
(sI-A)^{-1}B
=\frac{1}{s^2+a_1s+a_0}
\begin{bmatrix}1\\s\end{bmatrix}.
$$

左乘 C：

$$
C(sI-A)^{-1}B
=\frac{b_0+b_1s}{s^2+a_1s+a_0}.
$$

因此分母系数进入 A，输入通道进入 B，分子系数通常进入 C。

状态变量的选取并不唯一，所以同一个传递函数可以有不同的 A、B、C；只要它们给出的输入输出关系相同，就是等价实现。

# 2. 状态转移矩阵与状态响应

## 2.1 定义 2：状态转移矩阵

$$
\Phi(t):=e^{At}.
$$

## 2.2 定理 1：齐次系统的解

对

$$
\dot x=Ax
$$

，有

$$
x(t)=e^{At}x(0).
$$

**证明**　令

$$
x(t)=e^{At}x(0)
$$

。由 A3，

$$
\dot x=Ae^{At}x(0)=Ax(t),\qquad x(0)=Ix(0).
$$

由 A5 的唯一性，该函数就是系统解。证毕。

## 2.3 定理 2：受迫系统的状态响应

对

$$
\dot x=Ax+Bu
$$

，有

$$
\boxed{x(t)=e^{At}x(0)+\int_0^t e^{A(t-\tau)}Bu(\tau)\,d\tau.}
$$

**证明**　左乘积分因子

$$
e^{-At}
$$

$$
\frac{d}{dt}\bigl(e^{-At}x(t)\bigr)=e^{-At}Bu(t).
$$

从

$$
0
$$

到 t 积分，再左乘

$$
e^{At}
$$

，即得结论。证毕。

第一项是零输入响应，第二项是零状态响应。

**例 2**　若

$$
A=0
$$

$$
B=1
$$

$$
x(0)=2
$$

$$
u(t)=3
$$

，则

$$
x(t)=2+\int_0^t3\,d\tau=2+3t.
$$

# 3. 拉普拉斯域、传递函数与极点

## 3.1 命题 2：状态方程的拉普拉斯解

$$
X(s)=(sI-A)^{-1}x(0)+(sI-A)^{-1}BU(s).
$$

**证明**　对状态方程作拉普拉斯变换：

$$
sX(s)-x(0)=AX(s)+BU(s).
$$

移项得

$$
(sI-A)X=x(0)+BU
$$

，左乘

$$
(sI-A)^{-1}
$$

。证毕。

## 3.2 推论 1：状态空间到传递函数

在零初值下，

$$
\boxed{G(s)=\frac{Y(s)}{U(s)}=C(sI-A)^{-1}B+D.}
$$

**证明**　由命题 2，

$$
X=(sI-A)^{-1}BU
$$

；代入

$$
Y=CX+DU
$$

并提取 U。证毕。

## 3.3 命题 3：内部模态与特征值

由于

$$
(sI-A)^{-1}=\frac{\mathrm{adj}(sI-A)}{\det(sI-A)},
$$

A 的特征值由

$$
\det(sI-A)=0
$$

给出，并决定内部模态。若实现最小，则它们也是传递函数极点；若实现非最小，传递函数中可能发生极零抵消。

**例 3**　对例 1 的 A，

$$
\det(sI-A)=
\begin{vmatrix}s&-1\\2&s+3\end{vmatrix}
=s^2+3s+2=(s+1)(s+2).
$$

故内部模态为

$$
e^{-t}
$$

与

$$
e^{-2t}
$$

# 4. 连续系统稳定性

## 4.1 定理 3：线性定常系统的渐近稳定判据

系统

$$
\dot x=Ax
$$

渐近稳定，当且仅当

$$
\mathrm{Re}\bigl(\lambda_i(A)\bigr)<0,
\qquad i=1,\ldots,n.
$$

**证明要点**　把 A 化为 Jordan 形。每个响应项都是

$$
t^k e^{\lambda_i t}
$$

；它趋于零当且仅当

$$
\mathrm{Re}(\lambda_i)<0
$$

。证毕。

**例 4**　例 1 的特征值为

$$
-1,-2
$$

，均在左半平面，故零输入状态趋于零。

# 5. 能控性

## 5.1 定义 3：完全能控

若存在有限时间

$$
T>0
$$

与输入

$$
u(t)
$$

，能把任意初态转移到任意终态，则

$$
(A,B)
$$

完全能控。

## 5.2 定义 4：能控矩阵

$$
\mathcal C=[B\ AB\ A^2B\ \cdots\ A^{n-1}B].
$$

## 5.3 定理 4：Kalman 能控秩判据

$$
\boxed{\mathrm{rank}(\mathcal C)=n.}
$$

即

$$
(A,B)
$$

完全能控当且仅当上式成立。

**证明要点**　由定理 2，零初态可达状态属于
$$
\mathrm{span}\{B,AB,A^2B,\ldots\}
$$

。由 A4，高次项不会产生超出
$$
\mathrm{span}\{B,AB,\ldots,A^{n-1}B\}
$$

的新方向。因此可达子空间恰为

$$
\mathcal C
$$

的列空间；它等于

$$
\mathbb{R}^n
$$

当且仅当

$$
\mathcal C
$$

满行秩。证毕。

**例 5**　取

$$
A=\begin{bmatrix}0&1\\-2&-3\end{bmatrix},\qquad
B=\begin{bmatrix}0\\1\end{bmatrix}.
$$

则

$$
AB=\begin{bmatrix}1\\-3\end{bmatrix},\qquad
\mathcal C=\begin{bmatrix}0&1\\1&-3\end{bmatrix},\qquad
\det\mathcal C=-1\ne0.
$$

故系统完全能控。

## 5.4 反例 1：稳定不等于能控

若

$$
A=\mathrm{diag}(-1,-2)
$$

$$
B=[1,0]^T
$$

，则
$$
\mathcal C=[B,AB]
$$

的秩为 1。第二个状态虽然稳定，却不能由输入改变。

# 6. 状态反馈与极点配置

## 6.1 定义 5：状态反馈

取

$$
u=-Kx+v,
$$

则

$$
\dot x=(A-BK)x+Bv.
$$

## 6.2 命题 4：反馈改变闭环矩阵

闭环极点是

$$
A-BK
$$

的特征值。若

$$
(A,B)
$$

完全能控，则可用适当 K 任意配置全部闭环极点。

**例 6**　对双积分器

$$
A=\begin{bmatrix}0&1\\0&0\end{bmatrix},\quad
B=\begin{bmatrix}0\\1\end{bmatrix},\quad K=[k_1\ k_2],
$$

有

$$
A-BK=\begin{bmatrix}0&1\\-k_1&-k_2\end{bmatrix},
$$

其特征多项式为

$$
s^2+k_2s+k_1
$$

。若希望极点为

$$
-2,-3
$$

，则

$$
(s+2)(s+3)=s^2+5s+6,
$$

故

$$
K=[6,5]
$$

# 7. 能观性

## 7.1 定义 6：完全能观

若在有限时间内，可由已知输入与输出唯一确定初始状态，则

$$
(A,C)
$$

完全能观。

## 7.2 定义 7：能观矩阵

$$
\mathcal O=
\begin{bmatrix}
C\\CA\\CA^2\\\vdots\\CA^{n-1}
\end{bmatrix}.
$$

## 7.3 定理 5：Kalman 能观秩判据

$$
\boxed{\mathrm{rank}(\mathcal O)=n.}
$$

即

$$
(A,C)
$$

完全能观当且仅当上式成立。

**证明**　先令已知输入为零。由

$$
y=Cx
$$

与

$$
\dot x=Ax
$$

$$
y^{(k)}=CA^kx.
$$

故

$$
y,\dot y,\ldots,y^{(n-1)}
$$

给出线性方程

$$
\begin{bmatrix}y&\dot y&\cdots&y^{(n-1)}\end{bmatrix}^T=\mathcal O x.
$$

该方程对 x 有唯一解当且仅当

$$
\mathcal O
$$

满列秩。已知输入的贡献可以先从输出中扣除。证毕。

**例 7**　对例 1，取

$$
C=[1,0]
$$

，则

$$
\mathcal O=\begin{bmatrix}1&0\\0&1\end{bmatrix},
$$

故完全能观。

## 7.4 推论 2：对偶性

$$
(A,B)\Longleftrightarrow(A^T,B^T).
$$

左侧表示能控，右侧表示能观。

$$
B^T
$$

在对偶系统中充当输出矩阵，因为对偶系统的能观矩阵正是

$$
\mathcal C^T
$$

# 8. 全维状态观测器

## 8.1 定义 8：Luenberger 观测器

$$
\dot{\hat x}=A\hat x+Bu+L(y-C\hat x).
$$

## 8.2 定理 6：观测误差动力学

令

$$
e=x-\hat x
$$

，若

$$
D=0
$$

，则

$$
\boxed{\dot e=(A-LC)e.}
$$

**证明**

$$
\dot e=(Ax+Bu)-\left[A\hat x+Bu+L(y-C\hat x)\right]
$$

$$
\dot e=A(x-\hat x)-LC(x-\hat x)
$$

$$
\dot e=(A-LC)e.
$$

若

$$
(A,C)
$$

完全能观，可配置

$$
A-LC
$$

的全部极点，使

$$
e(t)\to0
$$

**例 8**　对双积分器取

$$
C=[1,0]
$$

$$
L=[l_1,l_2]^T
$$

，则

$$
A-LC=\begin{bmatrix}-l_1&1\\-l_2&0\end{bmatrix},
$$

特征多项式为

$$
s^2+l_1s+l_2
$$

。希望观测器极点为

$$
-5,-6
$$

，得

$$
L=[11,30]^T
$$

# 9. 分离原理

## 9.1 定理 7：控制器与观测器可独立设计

采用

$$
u=-K\hat x+Nr,
$$

并令

$$
e=x-\hat x
$$

，增广系统可写成上三角块形式，其对角块为
$$
A-BK
$$

与

$$
A-LC
$$

。因此闭环特征值集合为

$$
\sigma(A-BK)\cup\sigma(A-LC).
$$

**证明要点**　将

$$
\hat x=x-e
$$

代入真实系统，与误差方程联立；块上三角矩阵的特征多项式等于两个对角块特征多项式之积。证毕。

# 10. 参考前馈校正（预补偿）

## 10.1 术语校正

常见称谓是**参考前馈校正**、**前置补偿**或

$$
N_{\mathrm{bar}}
$$

；“提前矫正”容易误解为超前校正。它不改变

$$
A-BK
$$

的极点，只校准参考输入到稳态输出的静态增益。

## 10.2 定理 8：SISO、D = 0 时的前馈系数

设

$$
u=-Kx+Nr,
$$

$$
A-BK
$$

可逆且闭环稳定。若 r 为常值，希望

$$
y_{ss}=r
$$

，则

$$
\boxed{N=-\left[C(A-BK)^{-1}B\right]^{-1}.}
$$

**证明**　稳态时

$$
\dot x=0
$$

，故

$$
0=(A-BK)x_{ss}+BNr,
$$

从而

$$
x_{ss}=-(A-BK)^{-1}BNr.
$$

于是

$$
y_{ss}=-C(A-BK)^{-1}BNr.
$$

令

$$
y_{ss}=r
$$

，消去 r 即得。证毕。

**例 9**　沿用例 6，并取

$$
C=[1,0]
$$

$$
K=[6,5]
$$

。计算得

$$
C(A-BK)^{-1}B=-\frac16,
$$

故

$$
N=6
$$

。当

$$
r=2
$$

时，理想稳态输出为 2。

## 10.3 定理 9：D ≠ 0 时的公式

若

$$
y=Cx+Du
$$

，则稳态参考增益为

$$
H_0=D-(C-DK)(A-BK)^{-1}B.
$$

若

$$
H_0
$$

为方阵且可逆，则

$$
\boxed{N=H_0^{-1}.}
$$

**证明**　将

$$
x_{ss}=-(A-BK)^{-1}BNr
$$

与
$$
u_{ss}=-Kx_{ss}+Nr
$$

代入输出式，整理得

$$
y_{ss}=H_0Nr
$$

。证毕。

## 10.4 MIMO 与不可逆情形

对多输入多输出系统，只有当相应直流增益方阵且非奇异时，才能直接取逆。若不是方阵或秩不足，应求满足

$$
H_0N=I
$$

的右逆，或改用最小二乘、伪逆、积分器或稳态调节方程。不存在解时，静态前馈无法让所有输出同时无差跟踪。

## 10.5 前馈与积分控制的边界

前馈 N 依赖模型，只消除名义模型下的常值参考比例误差；参数偏差和恒值扰动仍可产生稳态误差。积分控制利用误差反馈，能在闭环稳定且调节条件满足时抵抗这类误差。

# 11. 李雅普诺夫稳定性

## 11.1 定义 9：二次型候选函数

取

$$
V(x)=x^TPx,\qquad P=P^T>0.
$$

## 11.2 定理 10：连续 Lyapunov 判据

矩阵 A 是 Hurwitz，当且仅当对任意

$$
Q=Q^T>0
$$

，方程

$$
A^TP+PA=-Q
$$

存在唯一解

$$
P=P^T>0
$$

**充分性证明**　沿

$$
\dot x=Ax
$$

$$
\dot V=\dot x^TPx+x^TP\dot x
=x^T(A^TP+PA)x=-x^TQx<0
$$

对任意

$$
x\ne0
$$

成立，故原点渐近稳定。证毕。

**例 10**　标量系统

$$
\dot x=-2x
$$

，取

$$
Q=1
$$

。由
$$
-4P=-1
$$

得

$$
P=1/4>0
$$

，且

$$
\dot V=-x^2<0
$$

# 12. 线性二次型最优控制（LQR）

## 12.1 定义 10：性能指标

$$
J=\int_0^\infty(x^TQx+u^TRu)\,dt,
\qquad Q\succeq0,\ R\succ0.
$$

## 12.2 定理 11：连续时间 LQR

若系统满足标准可稳定、可检测条件，则最优控制为

$$
\boxed{u=-Kx,\qquad K=R^{-1}B^TP,}
$$

其中 P 是代数 Riccati 方程的稳定化解：

$$
\boxed{A^TP+PA-PBR^{-1}B^TP+Q=0.}
$$

**证明要点**　令最优值函数

$$
V=x^TPx
$$

。Hamilton--Jacobi--Bellman 方程为

$$
0=\min_u\{x^TQx+u^TRu+2x^TP(Ax+Bu)\}.
$$

对 u 求导并令零：

$$
2Ru+2B^TPx=0
$$

，得
$$
u=-R^{-1}B^TPx
$$

；代回并比较二次型系数即得 Riccati 方程。证毕。

**例 11**　标量系统

$$
\dot x=u
$$

，取

$$
Q=R=1
$$

。Riccati 方程为
$$
-P^2+1=0
$$

，稳定化解

$$
P=1
$$

，故

$$
u=-x
$$

# 13. 连续系统的零阶保持离散化

## 13.1 定理 12：精确离散模型

采样周期为 T，且每个采样区间内输入保持常值，则

$$
\boxed{x_{k+1}=A_dx_k+B_du_k,}
$$

其中

$$
\boxed{A_d=e^{AT},\qquad B_d=\int_0^T e^{A\tau}B\,d\tau.}
$$

**证明**　对定理 2 在区间

$$
[kT,(k+1)T]
$$

应用状态响应公式，并令区间内

$$
u(t)=u_k
$$

。变量替换后即得。证毕。

**例 12**　积分器

$$
\dot x=u
$$

中

$$
A=0,B=1
$$

，故
$$
A_d=1
$$

$$
B_d=T
$$

，即

$$
x_{k+1}=x_k+Tu_k
$$

# 14. 一条完整推理链

由 A1--A5，现代控制的主线为

$$
\dot x=Ax+Bu
\Rightarrow e^{At}
\Rightarrow (sI-A)^{-1}
\Rightarrow \lambda(A)
\Rightarrow \mathcal C,\mathcal O
\Rightarrow A-BK,A-LC.
$$

在此基础上：

1. K 决定闭环动态；
2. L 决定估计误差动态；
3. N 校准名义稳态参考增益；
4. 积分器抵抗恒值误差；
5. LQR 在状态误差与控制能量之间求最优折中；
6. 离散化把连续模型转换为数字控制模型。

# 15. 计算检查表

## 15.1 建模

1. 选状态并检查是否足以决定未来；
2. 写出

    $$
    A,B,C,D
    $$
3. 检查矩阵维数。

## 15.2 分析

1. 极点：

    $$
    \det(sI-A)=0
    $$
2. 能控：

    $$
    \mathrm{rank}(\mathcal C)=n
    $$
3. 能观：

    $$
    \mathrm{rank}(\mathcal O)=n
    $$
4. 稳定：连续系统要求全部特征值实部小于零。

## 15.3 设计

1. 状态反馈：配置

    $$
    A-BK
    $$
2. 观测器：配置

    $$
    A-LC
    $$
3. 前馈：先确认闭环稳定、参考为常值、直流增益可逆；
4. 有扰动或模型误差：优先考虑积分作用；
5. 数字实现：使用

    $$
    A_d,B_d
    $$

    ，不要把连续矩阵直接当作离散矩阵。
